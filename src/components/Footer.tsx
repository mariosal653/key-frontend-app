const Footer = () => {
  return (
    <footer className="bg-gray-100 text-center py-4 text-sm text-gray-600 mt-auto">
      © {new Date().getFullYear()} KEY PROYECT. Todos los derechos reservados.
    </footer>
  );
};

export default Footer;
